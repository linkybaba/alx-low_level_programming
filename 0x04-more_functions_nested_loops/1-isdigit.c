#include "main.h"

/**
 *  _isdigit - check if is digit
 * @c: The number to be checked
 * Return: 1 if digit or 0 else
 */

int  _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}

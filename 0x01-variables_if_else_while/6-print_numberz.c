#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
	putchar(d);
	putchar('\n');
	return (0);

}
